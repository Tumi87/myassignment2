name: Test Workflow

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  say_hello:
    runs-on: ubuntu-latest
    steps:
      - name: Run a greeting
        run: echo "Hello! My workflow is finally running."
